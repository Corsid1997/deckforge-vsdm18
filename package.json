{
  "name": "deckforge-vsdm18",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:bridge": "pnpm --filter bridge dev",
    "install:all": "pnpm install"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
